# generated by containerlab at 2026-07-29T15:31:54+08:00
topology:
  defaults: {}
  nodes:
    requestflow-net-frontend:
      kind: ext-container
      exec:
      - ip address replace 10.10.30.10/24 dev eth1
      - ip route replace 10.10.10.0/24 via 10.10.30.1 dev eth1
    router:
      kind: linux
      image: ghcr.io/srl-labs/network-multitool:latest
      exec:
      - ip address replace 10.10.10.1/24 dev eth1
      - ip address replace 10.10.30.1/24 dev eth2
      sysctls:
        net.ipv4.ip_forward: "1"
    user-client:
      kind: linux
      image: ghcr.io/srl-labs/network-multitool:latest
      exec:
      - ip address replace 10.10.10.21/24 dev eth1
      - ip route replace 10.10.30.0/24 via 10.10.10.1 dev eth1
      - sh -c 'grep -q "requestflow.local" /etc/hosts || echo "10.10.30.10 requestflow.local"
        >> /etc/hosts'
  links:
  - type: veth
    mtu: 9500
    endpoints:
    - node: user-client
      interface: eth1
    - node: router
      interface: eth1
  - type: veth
    mtu: 9500
    endpoints:
    - node: router
      interface: eth2
    - node: requestflow-net-frontend
      interface: eth1
